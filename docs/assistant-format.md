# Формат общения и работы (обязателен)

## 🟩 ЧТО ДЕЛАТЬ МНЕ
- Только конкретные действия.
- Если есть блоки COPY INTO CURSOR / COPY INTO TERMINAL — всегда указывать порядок вставки и сколько нужно окон терминала.

## 🟦 ЧТО ЭТО ВСЕ СДЕЛАЕТ
- Одной-двумя строками: какой результат получим.

## 🟨 СОВЕТЫ
- Коротко, по делу.

## 🟥 ПОДВОДНЫЕ КАМНИ
- Коротко, что может пойти не так и как не сломать.

---

## Правило рабочих блоков (копипаст без сомнений)
Если ассистент предлагает что-то добавить/изменить/запустить — он выводит ТОЛЬКО готовые рабочие блоки для копирования:
- COPY INTO CURSOR — один блок с чистым промптом для Cursor (без лишнего текста внутри блока)
- COPY INTO TERMINAL — один sh-блок с командами (без лишнего текста внутри блока)

Обычное общение и пояснения — ТОЛЬКО вне блоков.

---

## Порядок вставки блоков (всегда)
Если в ответе есть оба блока:

1) Сначала вставить COPY INTO CURSOR в Cursor Agent.
2) Дождаться завершения (diff/результат/готово).
3) Затем вставить COPY INTO TERMINAL в терминал.

---

## Окна терминала
- По умолчанию: 1 окно/вкладка терминала.
- Если нужно 2 окна/вкладки, ассистент обязан явно написать:
  "НУЖНО 2 ОКНА: A и B" и что делать в каждом.
- Если этого нет — работаем строго в одном окне.
